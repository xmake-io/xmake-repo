diff --git a/CMakeLists.txt b/CMakeLists.txt
index ee48f6c..408888d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -216,6 +216,10 @@ if (BUILD_SHARED_LIBS)
   if (MSVC)
     add_definitions (-DSOXR_DLL)
   endif ()
+else ()
+  if (MINGW)
+    add_definitions (-DSOXR_VISIBILITY)
+  endif ()
 endif ()
 
 if (CMAKE_BUILD_TYPE STREQUAL "None") # As used by some distros.
