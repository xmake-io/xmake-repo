package("webview")

set_kind("library", {headeronly = true})

set_homepage("https://github.com/webview/webview")
set_description("A tiny cross-platform webview library for C/C++ to build modern cross-platform GUIs.")
set_license("MIT")

add_urls(...)
add_versions(...)

on_test(function(package)
    ...
end)
